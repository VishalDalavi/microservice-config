spring:
 application:
  name: Config-default
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: false
    regitry-with-eureka: false
    service-url:
      defaultZone: http://localhost:8761/eureka
